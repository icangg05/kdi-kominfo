; Harus >= config('app.upload.file_maks_kb') di config/app.php.
upload_max_filesize = 100M
; Sisa ruang untuk overhead multipart.
post_max_size = 110M
; GD membuka gambar ~4 byte per piksel: foto 24 MP sudah ~100 MB.
memory_limit = 512M
